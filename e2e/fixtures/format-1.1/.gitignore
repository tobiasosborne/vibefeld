# af proof workspace — generated by `af init`.
# Tracked (commit these): ledger/, assumptions/, externals/, meta.json.
# Everything below is runtime state or rebuildable from the ledger.
locks/
.af/
nodes/
defs/
lemmas/
