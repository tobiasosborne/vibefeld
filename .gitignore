# Binaries
/af
*.exe

# Test binaries (go test -c) and stray go build outputs at the repo root
*.test
/hooks

# Coverage profiles
coverage*.out
*.coverprofile

# TLC metadata when running TLC by hand (scripts/tla-check.sh uses a temp dir)
specs/tla/states/
