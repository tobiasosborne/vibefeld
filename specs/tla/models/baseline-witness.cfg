\* expect: violates Witness_AllCommit
\* Witness for baseline.cfg (same constants): every writer can commit, so the passing run is not vacuous.
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2}
  Reapers = {}
  Readers = {rd1}
  BatchSize = 2
  MaxAttempts = 2
  MaxCrashes = 0
  PidVisible = TRUE
  NoReplace = FALSE
  AtomicReaddir = TRUE
INVARIANTS
  Witness_AllCommit
