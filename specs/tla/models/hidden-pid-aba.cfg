\* expect: violates NoABA
\* Three writers, a reaper that cannot see the holder pid: a passing CAS can hide a rewritten prefix (ABA on the tail number).
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2, w3}
  Reapers = {r1}
  Readers = {}
  BatchSize = 1
  MaxAttempts = 1
  MaxCrashes = 0
  PidVisible = FALSE
  NoReplace = FALSE
  AtomicReaddir = TRUE
INVARIANTS
  NoABA
