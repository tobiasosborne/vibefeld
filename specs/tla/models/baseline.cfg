\* expect: pass
\* Two writers, one atomic reader, no reaper, no crash: the protocol as designed.
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2}
  Reapers = {}
  Readers = {rd1}
  BatchSize = 2
  MaxAttempts = 2
  MaxCrashes = 0
  PidVisible = TRUE
  NoReplace = FALSE
  AtomicReaddir = TRUE
INVARIANTS
  MutualExclusion
  NoLostAck
  NoGap
  BatchPrefix
  NoABA
  ReaderSeesNoGap
