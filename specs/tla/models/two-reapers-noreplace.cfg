\* expect: pass
\* Same race with a no-replace publish: mutual exclusion still breaks, but no acknowledged event is lost.
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2, w3}
  Reapers = {r1, r2}
  Readers = {}
  BatchSize = 2
  MaxAttempts = 2
  MaxCrashes = 1
  PidVisible = TRUE
  NoReplace = TRUE
  AtomicReaddir = TRUE
INVARIANTS
  NoLostAck
  NoGap
  BatchPrefix
  NoABA
