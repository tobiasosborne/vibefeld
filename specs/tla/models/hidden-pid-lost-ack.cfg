\* expect: violates NoLostAck
\* Holder pid not visible to the reaper (containers, other host): one reaper suffices to lose a write.
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2}
  Reapers = {r1}
  Readers = {}
  BatchSize = 1
  MaxAttempts = 1
  MaxCrashes = 0
  PidVisible = FALSE
  NoReplace = FALSE
  AtomicReaddir = TRUE
INVARIANTS
  NoLostAck
