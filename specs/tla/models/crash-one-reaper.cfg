\* expect: pass
\* A writer may die holding the lock; one reaper clears it (visible pids).
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2}
  Reapers = {r1}
  Readers = {}
  BatchSize = 2
  MaxAttempts = 2
  MaxCrashes = 1
  PidVisible = TRUE
  NoReplace = FALSE
  AtomicReaddir = TRUE
INVARIANTS
  MutualExclusion
  NoLostAck
  NoGap
  BatchPrefix
  NoABA
  ReaderSeesNoGap
