\* expect: violates MutualExclusion
\* Witness for two-reapers-noreplace.cfg (same constants): the reaper race still happens here; only its damage is contained.
SPECIFICATION Spec
CONSTANTS
  Writers = {w1, w2, w3}
  Reapers = {r1, r2}
  Readers = {}
  BatchSize = 2
  MaxAttempts = 2
  MaxCrashes = 1
  PidVisible = TRUE
  NoReplace = TRUE
  AtomicReaddir = TRUE
INVARIANTS
  MutualExclusion
